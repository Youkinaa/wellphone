# 项目协作与工程规范

## 1. 适用范围与阅读顺序

- 开始工作前，阅读项目 README、当前路径适用的 AGENTS.md，以及与任务直接相关的需求、设计、接口和运行说明。
- 使用项目已有的文档导航和开发入口；文件不存在时不猜测路径，也不为满足形式要求创建空文档。
- 区分三类信息：需求规定“应该怎样”，代码与运行证据说明“实际怎样”，计划描述“准备怎样”。不得混为一谈。
- 历史报告、注释和旧设计不能直接当作当前事实。引用实现结论前，核对当前代码、消费者或有效运行证据。
- 发现代码与需求不一致时，明确记录差异；不得通过修改文档、降低测试要求或删除验收条件，让问题表面消失。

本项目的设计依据按下表定位。开发前阅读主设计及受影响模块的契约；文档中的“尚未实现/待验证”不能当成现有能力。用户的新指令优先，涉及设计变化时在同一改动中同步依据。

| 权威文档 | 负责内容 |
| --- | --- |
| [主设计](docs/superpowers/specs/2026-09-21-wellphone-design.md) | 项目范围、总体架构、选型与 MVP 边界 |
| [运行时契约](docs/superpowers/specs/2026-09-21-agent-runtime-contracts.md) | 模块职责、触发条件、输入输出、数据和工具协议 |
| [执行流程](docs/superpowers/specs/2026-09-21-execution-flows.md) | 状态流转、时序、澄清/授权/人工接管与恢复 |
| [交互设计](docs/superpowers/specs/2026-09-21-interaction-app-design.md) | Web 页面、上传/对话、HTTP/SSE 和任务控制 |
| [验证计划](docs/validation/2026-09-21-feasibility-and-demo.md) | 验收条件；不以设计代替实测 |
| [环境准备](docs/validation/2026-09-21-real-app-preparation.md) / [开发日志](docs/development-journal.md) | 当前环境及历史实验；问题、难点、方案、结果与未决项 |

- 修改职责、接口、状态、触发条件或执行顺序时，同步对应表格/图、消费者及相关验证；不要只改代码，也不要复制第二份同义规范。
- 设计有实质空缺、互相冲突或外部行为超出授权时，及时说明具体缺口并沟通；独立且已授权的工作继续推进，常规实现细节自主决定。

## 2. 工作范围与自主推进

- 先明确任务目标、现有行为、受影响范围和完成标准，再实施修改。
- 在已授权范围内自主推进。常规实现选择、可逆修改和已经确认的原则，不反复请求批准。
- 信息不清楚时先检查文档、代码和证据；只有仍涉及实质取舍、范围冲突或未授权的外部影响时，才提出具体问题。
- 小修改保持聚焦，不顺带重构无关模块、升级依赖、统一格式或扩大需求。
- 跨模块改动先核对现有接口、未完成工作和并行修改，明确职责、输入输出及调用关系，再开始实施。
- 不因实现困难而静默缩减需求。不能完成时，说明实际阻碍、已完成部分和剩余工作。
- 持续推进到可验证的结果，不以给出计划、创建空壳或局部测试通过代替完成任务。

## 3. 工作区与工具使用

- 修改前检查工作区状态并阅读当前文件，保留用户和其他协作者已有的改动。
- 不擅自覆盖文件、撤销他人修改、丢弃未提交内容、重置分支或强制推送。
- 搜索优先使用 `rg`、`rg --files` 或环境中的等效工具。先确认真实路径与符号，再读取和修改。
- 按实际需要限制搜索范围和输出体量。输出被截断、命令失败或未读到的内容，不能算作已完成检查。
- 可以并行执行互不依赖的读取和检查；存在依赖的操作、共享文件写入和状态变更必须正确排序。
- 命令中的路径、参数和多行文本应正确转义，避免意外命令执行及敏感信息泄露。
- 使用项目既有的构建、格式化和检查方式，不擅自引入第二套工具链。

## 4. 职责、依赖与复杂度

- 每个模块应有清晰职责、输入输出和依赖方向。新增能力需要明确实际调用入口和消费者。
- 优先在已有职责归属中解决问题，避免多个模块分别维护同一规则、状态或事实。
- 将需要独立变化的配置与稳定执行逻辑分开，但不为了“通用化”把所有业务代码改成配置语言。
- 新增抽象、公共接口、依赖或基础设施必须服务于明确需求，不为假想扩展预建框架。
- 不以固定行数决定拆分。代码持续膨胀时，检查是否混合了不同职责、变化原因或依赖关系。
- 按职责和变化原因拆分，避免只把大函数搬进多个文件，仍保留原有耦合。
- 正常路径优先；必要的输入校验、权限检查和副作用约束应同时落实，不要求每一层重复包装错误。
- 异常类型、重试、持久状态、缓存、锁和兼容层应与实际契约及风险相称，不以“保险起见”为由不断叠加。
- 当前是 MVP：一个用户、后端进程、AVD、副屏和活动任务，优先完整正常路径。暂不建设多租户、分布式调度、通用风控/补偿框架或全场景异常矩阵；副屏隔离、停止/修改屏障、具体授权和未知提交禁止盲重试仍是题目所需边界。

## 5. 缺陷定位与修复

- 区分失败现象、触发条件和根因，不依据报错文案或单次现象直接追加分支。
- 优先建立最小失败复现，或用可靠的运行记录、调用链和数据流证明确切问题。
- 静态架构问题可以通过依赖关系、职责冲突和消费者分析证明，无须虚构运行故障。
- 明确输入在哪里被误解、丢失或改变，并在负责该语义的模块中修复。
- 不通过吞异常、跳过校验、放宽断言、无限重试或扩大超时来掩盖原因。
- 修复后使用同一失败条件验证，并检查直接消费者及必要的相邻行为。
- 删除被替代的错误分支和临时探针，避免永久保留新旧两套路径。
- 构造测试可以用于验证合法输入和契约边界，但必须与真实生产证据区分，不能将假设场景描述为已发生故障。
- 根因尚未确认时继续取证，明确标记不确定性，不把推测写成结论。

## 6. 重构、替换与退役

- 重构前明确需要保持的可观察行为：入口、消费者、支持输入、输出、持久状态、外部副作用、必要顺序及错误语义。
- 重构后逐项验证这些行为，确认测试实际覆盖了搬移或替换的分支。
- 新文件可以导入、新函数可以单独调用、测试整体通过，都不等于原有能力完整保留。
- 有意改变行为时单独说明需求及影响，不借“重构”名义混入未经确认的语义变更。
- 可以保留必要的公共接口或兼容入口，但应尽量委托同一实现，避免长期维护两份逻辑。
- 临时兼容和迁移路径应明确消费者、维护责任及退出条件；正式对外兼容遵循项目的版本策略。
- 删除旧实现时同步处理调用方、测试、配置、脚本和文档。
- 搜索不到静态引用，只能说明它是待核查对象；还需检查动态注册、命令行、插件、CI、运维及外部消费者。

## 7. 验证与证据

- 根据改动选择验证方式，优先检查被改模块、直接消费者和实际受影响的边界，并完成项目要求的必要检查。
- 测试关注行为、契约和关键回归，不机械复制实现，也不为纯文案等低影响修改增加无意义测试。
- 出现新的失败、风险或未解决疑点时再扩大验证范围；避免重复运行无关且昂贵的测试。
- 单元测试、模拟测试、回放、集成测试、现场验证和性能实验各有证明范围，不互相冒充。
- 测试通过不代表生产入口已经接通，也不代表真实外部系统接受了操作；必要时核对完整调用和结果消费链。
- 回放只能提供已记录的输入与反馈，不能证明未录制的新操作会在真实环境产生何种结果。
- 性能结论应说明测量范围和比较条件，明确区分实测结果与估算，不以局部收益代表端到端收益。
- 如实报告已执行、未执行、通过、失败及受阻的检查，不编造命令输出或验证结果。
- 可以复用同一代码和输入版本下仍然有效的证据；代码、环境或关键输入变化后，重新判断其适用性。

## 8. 文档与脚本维护

- 每类需求、设计或操作说明应有明确的权威位置，其余文档引用它，避免维护相互冲突的副本。
- AGENTS.md 保存稳定且普遍适用的规则；当前进度、具体方案、运行记录和个案分析放在项目已有的对应位置。
- 改变机制前，搜索该机制在代码、配置、注释、文档和脚本中的表述；修改后同步处理受影响的内容。
- 不仅检查“改了哪个文件”，还要检查“这个事实被哪些地方记录和使用”。
- 文档维护与改动规模相称，不要求每次小修复都新增设计报告、决策记录或完整流程文件。
- 保留脚本应有明确用途、真实消费者、使用入口和副作用说明。一次性诊断代码在问题结束后移除。
- 历史材料应明确其历史属性，不能继续作为当前行为的唯一依据。
- 删除或替换文档、脚本时检查引用和使用入口，不留下失效导航。
- 每个阶段收尾核对测试、测试替身、一次性探针、影子/兼容代码及旧文档的消费者。被正式路径替代的代码及时移除；仍有复现/答辩价值的材料标明历史状态并保留导航，未闭合问题的唯一证据不得清掉。不为了归档形式复制整套实现。

## 9. 数据、外部操作与清理

- 不提交或泄露密码、密钥、Token、Cookie、会话状态和其他凭据。业务数据按项目授权及数据要求处理。
- 外部写入、发布、部署和删除遵循现有授权；不得将本地测试授权推定为任意生产操作授权。
- 产品 Agent 的人工交接遵循执行流程：缺关键信息先澄清；发邮件/消息、提交订单、付款等须有覆盖具体对象、内容或金额的授权，先形成可审阅结果；已有有效具体授权不反复询问。登录/验证码等交用户亲自处理，不能自动抢主屏。超时不等于授权或失败。
- 超时或连接中断不等于操作没有生效。重试外部副作用前，按实际契约确认结果或使用有效的幂等机制。
- 修改持久状态应使用项目认可的入口，维护数据约束和审计关系，不绕过正常流程制造表面成功。
- 清理前确认当前消费者、维护责任和保留用途。文件名、年龄、忽略规则或没有打开句柄，都不足以单独证明文件无用。
- 并行工作期间，清理清单可能过期；执行删除前再次核对最新使用情况，避免删除其他工作的输入。
- 已完成且无消费者的临时测试数据、缓存副本和诊断输出应及时清理，保留必要结果及版本信息即可。
- 真实业务数据、在途任务、唯一采样和未闭合问题所需证据，不按普通临时文件删除。

## 10. 协作与审查

- 有多人或多个 Agent 协作时，明确任务边界、可写文件、接口约定和依赖，避免重复实施。
- 共享接口、集成入口和公共状态明确负责方，避免未经协调同时修改。
- 交接提供目标、约束、结果、必要证据和未解决项，不默认复制全部过程记录。
- 不将其他协作者的结论直接当作事实；涉及关键判断时核对对应代码或证据。
- 复杂或高风险修改在条件允许时安排独立审查，重点检查需求符合性、实际接线、行为保持和旧实现退役。
- 没有独立审查条件时如实说明，不将自查描述为独立验收。普通小修改不强制引入多角色流程。

## 11. 交付与经验沉淀

- 交付说明应包含：解决了什么、主要改动、验证结果，以及影响使用的剩余问题。
- 先说明结果和影响，再补充必要技术细节，避免以工具调用记录代替结论。
- 严格区分方案已确定、代码已实现、测试已通过、已部署和真实运行已验证。
- 若发现与本次任务无关的问题，简要记录影响和证据，不擅自扩大改动。
- 同类错误反复出现时，将其提炼为可执行、可检查的规则；记录共同原因，而不只罗列个案。
- 新增规则前检查是否已有同义约束，优先合并或修正。规则失效或不再适用时及时移除，避免 AGENTS.md 不断膨胀。
